# Capsule C001: Corpus Architecture Review

**Generated by:** Antigravity (Research Capability)
**Target Task:** T001-knowledge-engine
**Next Capability:** review

## Context
The Knowledge Engine requires a structured corpus of design references. We have completed the "Dry Run" phase by gathering 10 S/A-tier references and converting them into machine-readable YAML files located at `c:/WEB/REAL/PORTFOLIO/docs/framework/Core/corpus/`.

## Decisions Made
- Stored corpus entries as individual `.yaml` files instead of a single markdown file to allow easy parsing, programmatic extraction, and tagging.
- Enforced a strict taxonomy of tags (`hero`, `typography`, `layout`, `motion`, `dashboard`).

## Extracted Patterns (Preview)
- **Typography:** Bold sans-serif for H1 with tight tracking (Stripe); serif/sans-serif pairing for long-form (Reflect).
- **Layout/UI:** 1px subtle borders and glows for dark mode (Linear); 4px/8px baseline grid (Tailwind).
- **Motion:** Physics-based spring animations over linear easing (Family.co); scroll-linked reveals (Framer).

## Action Required for Next Agent (Reviewer)
1. Review the 10 YAML files in the `corpus/` directory.
2. Identify any missing fields or architectural gaps in the structure before we scale to 150 entries.
3. If approved, advance the task to scale the corpus or extract formal patterns into `PATTERNS.md`.
