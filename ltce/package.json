{
  "name": "@crtx/ltce",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "npm run build",
    "test": "vitest run",
    "lint": "eslint . --ext .ts",
    "typecheck": "npm run typecheck",
    "format": "prettier --write \"**/*.ts\""
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "zod": "^4.4.3"
  }
}
