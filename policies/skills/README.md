# skills/

Shared skill/prompt definitions usable by both agents. For Claude-specific
skills, the canonical location is `.claude/skills/` — mirror or link
cross-agent-relevant ones here so Antigravity can read them too.

Add one `.md` file per shared skill, named after the skill.
